# OCRPix + AudioClean — Domain SEO Shortlist (June 2026)

> Data-backed `.com` domain picks for two new WorksOffline projects.
> Method: Google autocomplete keyword stems (38 queries) → bulk RDAP availability check (230 candidates) → trademark + premium-tier risk filter → tiered ranking by SEO ranking potential.
>
> All listed domains: RDAP-verified available as of 2026-06-06. Always verify on Namecheap/Porkbun before purchase (premium-tier pricing may apply for short/generic words).

---

## Project 1: OCRPix (Image / PDF → Text, fully client-side)

### Keyword cluster signal

Top keyword stems across all OCR autocompletes (out of 18 high-signal seed phrases):

```
 113× text         ← target this in stem
  69× ocr          ← target this in stem (technical)
  42× image        ← strong alternative
  34× pdf          ← high-signal subcluster
  20× screenshot   ← strong subcluster
  20× scanner      ← strong subcluster
  10× handwriting  ← premium-tier subcluster
```

**Killer phrase** (literal Google autocomplete): `"pdf ocr online free no sign up"` — that's our entire pitch in 7 words. A domain like `pdfocrfree.com` captures it directly.

### `.com` competitive landscape

These are TAKEN (gives a sense of category density):
| Domain | Registered | Note |
|---|---|---|
| `imagetotext.io` | 2017 | Real product, ranks well |
| `freeocr.com` | 2002 | Old, established |
| `webocr.com` | 2002 | Old, established |
| `pdfocr.com` | 2009 | Established |
| `imageocr.com` | 2020 | Recent grab |
| `snapocr.com` | 2025 | Brand-new — someone else had the same idea |
| `img2text.com` | 2016 | Niche but established |
| `imgtotext.com` | 2024 | Recent grab |

So clean keyword-direct `.com`s are gone. Best remaining: keyword-direct compounds.

### 🏆 OCRPix shortlist — `.com` only, ranked

#### Tier S — keyword-direct + brandable (will rank fastest)

| # | Domain | Target keyword cluster | Why it wins |
|---|---|---|---|
| 1 | **`browserocr.com`** ⭐ | `ocr in browser`, `browser ocr`, `ocr no upload` | **The wedge is in the domain.** "Browser" = client-side = privacy. Tells the differentiator AND has the keyword. Ranks for both "browser ocr" intent + "in browser ocr" question intent. |
| 2 | **`snappyocr.com`** | `ocr fast/quick`, `instant ocr`, `ocr now` | Brandable adjective + keyword. "Snappy" = speed promise. Memorable. Easy to grow brand beyond OCR (could become Snappy Tools later). |
| 3 | **`ocrinstant.com`** | `instant ocr`, `ocr now` | Compound captures "instant" intent. Less brandable but pure SEO. |

#### Tier A — pure SEO bait (high-intent capture)

| # | Domain | Target | Why |
|---|---|---|---|
| 4 | **`pdfocrfree.com`** | `pdf ocr online free no sign up` (THE killer autocomplete) | Literal exact-match for the highest-intent search. Ugly brand but if SEO is the only goal, this wins. |
| 5 | **`screentotext.com`** | `screenshot to text` cluster (10 autocompletes) | Captures the strongest mobile-pain cluster. People take screenshots constantly. |
| 6 | **`textfromimg.com`** | `extract text from image` cluster (10 autocompletes) | Action-form matches verb-intent searches. |
| 7 | **`shot2text.com`** | `screenshot to text` cluster | Pun on screenshot. `2` substitution is established convention for "to". |
| 8 | **`ocrpdfonline.com`** | `pdf ocr online` cluster | Less catchy than #4 but same SEO target. |

#### Tier B — premium / handwriting angle

| Domain | Why |
|---|---|
| `scribbleocr.com` | Handwriting-specific brand for premium tier (transformers.js TrOCR model). Cute, memorable. |
| `writingocr.com` | Same angle, less catchy. |
| `ocrway.com` | Short + brand-able. Might be premium-tier priced (~$30-50) due to length. |

### 🎯 My #1 OCR pick: **`browserocr.com`**

- **Why:** Tells the wedge ("browser" = no upload = privacy) AND has the keyword ("ocr") in 10-character stem.
- **SEO target:** "ocr in browser" (real autocomplete), "browser ocr", "ocr no upload", plus halo on "free ocr" searches.
- **Brand stretch:** Easy — could become BrowserOCR + BrowserCompress + BrowserConvert family if we want a sister brand to "Pix".
- **Risk:** Compound noun = should be ~$15. No trademark issues. No premium tier likely.
- **Backup:** `snappyocr.com` (more brandable, less keyword-direct)

---

## Project 2: AudioClean (RNNoise-style audio cleaner, fully client-side)

### Keyword cluster signal

Top stems (out of 15 high-signal seed phrases):

```
 77× noise        ← THE target word
 69× audio        ← target this
 69× remove/-al   ← action verb cluster
 23× background   ← "background noise" common phrase
 21× video        ← also targets video audio cleanup
 19× alternative  ← intent: looking for cheaper option
 18× podcast      ← strong subcluster (podcaster audience)
 17× adobe        ← people search "adobe podcast alternative"
 17× voice        ← strong subcluster
 13× clean        ← good action verb
 12× enhancer     ← related cluster
 10× krisp        ← people search "krisp alternative"
 10× echo         ← specific pain
 10× wind         ← specific pain
```

**Two killer phrases** (literal autocompletes):
- `"krisp alternative free"` — buyers looking for cheaper than $12/mo Krisp
- `"adobe podcast alternative free"` — same intent for Adobe Podcast Enhance

### Trademark warning ⚠

Any `.com` with `krisp` or `adobe` in the stem = TRADEMARK RISK. Even though autocomplete shows people search those terms, you cannot legally own `krispalternative.com` or `adobepodcastalt.com`. **I cut all of those from the shortlist** even though they came back as RDAP-available.

### `.com` competitive landscape

| Domain | Registered | Note |
|---|---|---|
| `denoise.com` | 2000 | Generic word, parked or premium |
| `cleanvoice.com` | 2011 | Active competitor (Cleanvoice.io is the brand) |
| `cleanaudio.com` | 2003 | Old |
| `audioclean.com` | 2004 | Old |
| `noisefree.com` | 1999 | Old |
| `audiocleaner.com` | 2024-11 | RECENT — someone built or squatted recently |

Clean keyword-direct singles are GONE. Best remaining: compound keyword stems.

### 🏆 AudioClean shortlist — `.com` only, ranked

#### Tier S — keyword-direct + brandable

| # | Domain | Target keyword cluster | Why it wins |
|---|---|---|---|
| 1 | **`denoisepix.com`** ⭐ | `denoise audio`, `audio denoiser`, `ai noise removal` | **Technical term = trust signal for podcasters.** "Denoise" is what audio engineers actually call this. Pairs with HEICPix branding family. |
| 2 | **`podcastclean.com`** | `podcast audio cleanup` (10 autocompletes) + 18 "podcast" stems | **Niches us to podcasters** — clean wedge. Podcasters are evangelists, pay for tools, share on Twitter. |
| 3 | **`voicecleanpix.com`** | `clean voice`, `voice cleaner`, `voice cleanup ai` | Targets the voice subcluster (vs. music/full-audio). Family suffix. |
| 4 | **`cleanaudiopix.com`** | `clean audio`, `audio cleaner`, `audio cleanup` | Broadest keyword target. Generic enough to grow. |

#### Tier A — verb-action brands

| # | Domain | Target | Why |
|---|---|---|---|
| 5 | **`vocalclean.com`** | `vocal clean`, `clean vocals` | Music-creator angle. Could be sister tool to VocalRip if we go karaoke later. |
| 6 | **`clarifyvoice.com`** | "clarify" intent | Poetic verb, brandable. Less SEO-direct. |
| 7 | **`polishaudio.com`** | `polish audio`, `audio polish` | Verb + noun, evocative. Less common search but strong brand. |
| 8 | **`podcastpurify.com`** | podcaster cluster | Niche brand if we go podcaster-first. |
| 9 | **`scrubaudio.com`** | `scrub audio` | Verb + noun. Slightly aggressive ("scrub") might appeal to engineers. |
| 10 | **`noisestrip.com`** | `strip noise` | Clear function statement. |

#### Tier B — quirky brands

| Domain | Why |
|---|---|
| `audiotidy.com` | British verb "tidy" = clean. Cute but weaker SEO. |
| `audiogroom.com` | "Grooming" metaphor for audio. Memorable. |
| `voicegroom.com` | Same. |

### 🎯 My #1 Audio pick: **`denoisepix.com`**

- **Why:** "Denoise" is the technical operation name that podcasters/audio engineers Google. Family suffix (-pix) ties it to HEICPix portfolio. Brandable. Clear function statement.
- **SEO target:** "denoise audio", "audio denoiser", "ai denoiser", plus halo on "noise removal" + "audio cleanup" searches.
- **Brand stretch:** Easy — could expand to denoise video, denoise image (Stable-Diffusion-style image denoising), etc. The -pix suffix already implies multi-media.
- **Risk:** ~$15 standard pricing expected. No trademark issues.
- **Backup:** `podcastclean.com` if you want to niche down to podcasters specifically.

---

## 🎯 Buy this combo

**Pre-spec purchase recommendation:**

1. **`browserocr.com`** (primary) + `snappyocr.com` (backup, optional ~$15)
2. **`denoisepix.com`** (primary) + `podcastclean.com` (backup if pivoting to podcaster-niche, ~$15)

**Total estimated cost: ~$60 if buying all 4, ~$30 if just buying the two primaries.**

### Where to buy

**Recommended order of preference:**
1. **Porkbun** — best `.com` pricing (~$10-12 first year), no upsells
2. **Namecheap** — slightly more expensive (~$13-15) but trusted UI, easy CF integration
3. **Cloudflare Registrar** — at-cost pricing if your CF account is already set up. Cheapest renewal cost ($10 for `.com`). Requires Cloudflare nameservers (auto for CF Pages, so this is actually ideal for us).

**My specific recommendation:** Buy via **Cloudflare Registrar** since you'll be hosting on Cloudflare Pages anyway. The wiring is one-click (no nameserver changes needed) and renewals are at-cost forever.

### Pricing sanity check before purchase

Some domains show RDAP-available but are tagged "premium" on registrars (Verisign price tier above standard). Watch for these signals:
- 3-4 character stems → often premium
- Single common-English-word stems → often premium
- "App"-friendly brand-like names → sometimes premium

**Of my picks above, all should be standard-tier (~$10-15).** But verify the cart price at checkout — if it shows >$50, it's premium-tier and I'd swap to the backup.

---

## What you get when these are bought

After buying, here's the immediate wiring (~20 min total for both):

1. **Add domain to Cloudflare** (if buying elsewhere — skip if Cloudflare Registrar)
2. **Create CF Pages project** for each, point to GitHub repo (skeleton can be empty)
3. **Add custom domain** in Pages settings — Cloudflare auto-provisions SSL
4. **Set `PUBLIC_SITE_URL` env var** to https://browserocr.com / https://denoisepix.com

After that, every push to `main` deploys to the real domain. SEO indexing can start day 1 instead of waiting for `.pages.dev` → migration later (the HEICPix mistake we made).

---

## Suggested next steps

(a) **Buy `browserocr.com` + `denoisepix.com` now** — I'll write both SPECs after
(b) **See live prices on Cloudflare Registrar first** — I can give you the URL to check before buying
(c) **More creative variants** — if none of these resonate, I can do another round with different naming patterns
(d) **Single domain instead** — buy one (OCRPix or AudioClean) and spec/ship that first; revisit the other after

---

*Generated by Hermes. RDAP availability checked 2026-06-06. Re-verify pricing on registrar before purchase.*
